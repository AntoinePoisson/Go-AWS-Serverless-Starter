{
  "name": "go-aws-serverless-starter",
  "version": "0.1.0",
  "description": "AWS Lambda + API Gateway + DynamoDB starter written in Go",
  "license": "MIT",
  "private": true,
  "scripts": {
    "prepare": "lefthook install"
  },
  "devDependencies": {
    "@commitlint/config-conventional": "^21.2.0",
    "@redocly/cli": "^2.45.0",
    "commitlint": "^21.2.1",
    "lefthook": "^2.1.10",
    "serverless": "^3.40.0",
    "serverless-iam-roles-per-function": "^3.2.0"
  }
}
